# task-scheduler
java task scheduler using @scheduler

by **sigitisme**

 - perMinuteTask -> runs every minutes but starts only if the second is 00
 - perSecondTask -> runs every seconds and starts immediately
